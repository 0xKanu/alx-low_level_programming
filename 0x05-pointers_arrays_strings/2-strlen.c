#include "main.h"
/**
  * _strlen - function that returns the length of a string.
  * @s: pointer of parameter.
  *
  * Return: length of string.
  */
int _strlen(char *s)
{
	int length = 0;

	while (s[length] != '\0')
	{
		n++;
	}

return (length);	
}
