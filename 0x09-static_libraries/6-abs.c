#include "main.h"
/**
  * _abs - computes the absolut value of an integer
  * @n: integer variable
  *
  * Return: 0
  */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
