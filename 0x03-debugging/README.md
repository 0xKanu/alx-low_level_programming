Short description of the Debugging project:
* main.h contains functions' prototypes.
* 0-main.c tests the function positive_or_negative() gives the correct output when given a case of 0.
* 1-main.c comment out (don’t delete it!) the part of the code that is causing the output to go into an infinite loop.
* 2-
