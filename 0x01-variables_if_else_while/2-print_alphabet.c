#include <stdio.h>
/**
  * main - this function prints the alphabet in lowercase.
  * Return: 0 everytime when if successful.
  */
int main(void)
{
	char alpha = 97;

	while (alpha <= 122)
	{putchar(alpha);

	alpha++;
	}

	putchar(10);

return (0);
}
