/*COde to print with the printf() function*/
#include <stdio.h>

/**
 *main - prints "with proper grammar, but the outcome is a piece of art,"
 *Return: 0 if the number is positive. non-zero otherwise
 **/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,","%s/n");
return (0); /*return 0 of true*/
}
